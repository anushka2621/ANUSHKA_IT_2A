#include<stdio.h>
void main()
{
char str1[100],str2[100];
int i ,c=0;
printf("enter the first string:");
gets(str1);
printf("enter the second string:");
gets(str2);
if(strlen1(str1)==strlen(str2)
{
    for(i=0;str1)
}}
